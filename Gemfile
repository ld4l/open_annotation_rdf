source 'https://rubygems.org'

gemspec

# GETTING FROM GEMFILE UNTIL MintLocalName CODE IS PUSHED INTO MASTER, THEN MOVE THIS BACK TO *.gemspec FILE
# Use active-triple for handling of triple persistence
gem 'active-triples', :git => 'git@github.com:no-reply/ActiveTriples.git', :branch => 'generate_id'

# GETTING FROM GEMFILE UNTIL LD4L::FoafRDF GEM IS RELEASED, THEN MOVE TO *.gemspec FILE
# Use LD4L::FoafRDF Gem
gem 'ld4l-foaf_rdf', '~> 0.0.2', :git => 'git@github.com:ld4l/foaf_rdf.git'
